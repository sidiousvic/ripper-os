# MacroFactor bodyweight fixture

This anonymized fixture preserves the verified MacroFactor sheet shapes used by the supplied export:
`Scale Weight` and `Weight Trend`, alongside minimal training sheets so the workbook is accepted.
Values are synthetic and are not copied from a personal archive.
